// Re-export everything from the tailwind-class-sorter package
export {
	sortClassString,
	buildMatchers,
	getTextMatch,
	processText,
	processFile,
	Options,
	LangConfig,
	Matcher
} from 'tailwind-class-sorter';
